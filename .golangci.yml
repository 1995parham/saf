---
version: "2"
linters:
  default: all
  disable:
    # deprecated since golangci-lint v2.13.0, replaced by exhaustruct_v5
    - exhaustruct
    - gomodguard
    - wsl
    - depguard
    - tagliatelle
    - nolintlint
    - noinlineerr
    - varnamelen
    - ireturn
    - revive
    - wrapcheck
    - gomoddirectives
    - gosec

  settings:
    wrapcheck:
      ignore-sigs:
        - .JSON
        - .NewHTTPError
        - .Redirect
        - .NoContent
        - .Errorf(
        - errors.New(
        - errors.Unwrap(
        - .Wrap(
        - .Wrapf(
        - .WithMessage(
        - .WithMessagef(
        - .WithStack(
